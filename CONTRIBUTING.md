# 参与贡献

感谢你愿意改进 Logion。本项目面向个人和最多 10 人的小规模自托管协作，提交应保持范围清晰、可验证，并尊重安全和数据边界。

## 开始之前

1. 搜索现有 Issue 和 Pull Request，避免重复工作。
2. 对权限、同步、数据模型、加密或部署语义的重大改变，先创建 Issue 说明使用场景、风险和兼容性影响。
3. 安全漏洞不要公开披露，请遵循 [SECURITY.md](SECURITY.md)。
4. 参与项目即表示同意 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。

## 项目范围

欢迎：

- 缺陷修复、可访问性和性能改进
- 离线、同步、数据可移植性与安全增强
- 清晰的文档、测试和开发体验改进
- 适合小规模自托管协作的功能

不接受：

- 计费、定价、套餐或 entitlement 系统
- 公共营销站、增长追踪或 SaaS 运营后台
- 绕过人工确认、服务端权限或审计边界的自动化
- 未经设计与迁移说明的数据契约破坏性变更

## 开发环境

需要 Node.js 24.14+、pnpm 11.9+、Python 3.12、uv；完整集成测试还需要 Docker Compose。

```bash
pnpm install --frozen-lockfile
uv sync --all-packages --group dev --frozen
pnpm ci:fast
```

更多启动方式见 [README](README.md#本地开发)。

## 变更原则

- 保持最小改动，不顺带重构无关模块。
- 复用现有 API、错误响应、权限和同步模式。
- 新增行为需要覆盖成功、失败、越权和边界条件。
- 不在日志、测试夹具或文档中提交真实邮箱、令牌、密钥或个人数据。
- OpenAPI 或同步契约变化必须重新生成并检查契约文件。
- 依赖升级应更新锁文件，并通过高危漏洞审计。

## 提交 Pull Request

Pull Request 请包含：

- 问题与解决方案的简要说明
- 影响的功能、数据和安全边界
- 实际运行的测试及结果
- 有意的契约、迁移或配置变化
- UI 变化的截图或录屏（如适用，且不得包含隐私数据）

保持提交信息简洁、使用祈使语气并描述结果，例如 `fix: preserve invite privacy during registration`。

维护者可能要求拆分过大的变更、补充测试或更新 ADR。所有检查通过并不自动代表变更一定合并。
