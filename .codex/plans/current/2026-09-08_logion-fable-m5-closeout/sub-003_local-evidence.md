# M5 本地证据续验

- 父计划：[M5 续跑计划](../2026-09-08_logion-fable-m5-closeout.md)，步骤 2、3、4。
- 状态：本批本地工作与自检完成；M5 总验收未通过，父计划保留 current。所有者要求继续未通过项，沿用既有本地测试和修复授权。
- 基线与产品：沿用 R-11 固定候选，18 份产品摘要 `c92d51c279db6c8434f399f628b2c0f41cb1ab567c0d0fe06ca04d9a7706fd6a`。新增测试独立归因，旧候选及证据保留。
- 边界：真实 Provider 三次、邮件五封额度均用完；本批零新增外呼、零发信，不操作生产或 Git 交付，不改 T-04 保护文件。
- MCP 任务工具不可用；同步本计划及当前 Run，不补造 actor、assignment 或 accepted 事件。

## 步骤

1. 已完成：新增九项邮件表单回归；与八项 token 回归合计十七项通过。首轮九项失败来自新测试没有等待 microtask 更新，修正测试等待后通过，原失败保留。整包 Web 四百二十五项、typecheck 通过；合成测试不计为真实邮箱按钮或浏览器最终提交通过。
2. 本轮未复现、风险保留：同产品隔离目录整包 Web 通过，十九项 AI workbench 均通过。代码核对确认两阶段加载，但没有证明旧超时根因；产品及既有测试等待未改，不将复跑成功记为稳定性问题已修复。
3. 已完成本批：Provider 两项集成与新增九次拒绝、写入不变和假 adapter 零调用通过，独占容器及卷已清理。结构化清点 OpenAPI 153 path、186 operation，模块归因已写收口报告；完整 API IDOR 仍需逐操作矩阵，不由本模块扩大认定。
4. 已完成：清单 2.3/A-11 过时归因已修正；53 文件、115 本地链接、摘要、保护、格式/Ruff/ESLint、秘密、空暂存区与 Run 复核通过，Web/API 健康。原 39 文件候选完整 CI 保留，两补充测试合并选择 41 文件摘要 `2946c256e4ed705ee7c8412688333e160f07e8eff4187bfaa0cca39599d21f41`；M5 未通过、M6 未授权。

仓库外最终结果：`m5-local-evidence-review-GK6rtY/review-check.json` 与
`supplement-manifest.json`。测试细节见收口报告 LOC-01–LOC-04，旧失败证据保留。

## 设备限制

本轮 Computer Use 能列出 Chrome 窗口，但读取窗口时被自动停止，原因是无法可靠确认
浏览器 URL。没有启动 NVDA，没有执行桌面输入，不改用其他桌面入口规避该停止。
实际语音播报仍待可用设备或所有者协作；本地组件、API 和静态检查继续。
