# M5 Provider TLS 兼容修复子计划

- 状态：已完成修复及自检，返回父计划步骤 3；不代表 M5 或用户最终验收通过。
- 父计划：步骤 3 外部验收批次。
- 范围：两份 AI adapter 的 SNI 参数、对应单元断言、真实本地 TLS 回归。
- 原候选保持；修复后创建新候选，重新绑定摘要与必要检查。

## 步骤

1. 已完成：本地 HTTPS 服务在旧实现四项复现真实异常，确认旧 MockTransport 绕过 TLS。
2. 已完成：两处 SNI 改为字符串；正常握手和错误证书拒绝通过，公网 DNS/IP 固定等边界保持。
3. 已完成：目标 86 项、Ruff 与新候选完整 CI 八门通过；真实发现和一次合成生成均 200，27 项检查通过。
4. 已完成：结果和未验边界写入父计划及收口报告，旧失败/候选与三轮独占环境清理证据保留。

## 已观察

- 原候选真实发现进入 TLS 后抛出 AttributeError，未收到 Provider HTTP 响应。
- 首轮独占数据库和 Redis 已清理；凭据未进入脚本、仓库或报告。
- 最终真实生成输入 177 / 输出 332 tokens，应用按保守价格记账 1 分；不等同于账单对账。
- 首轮 TLS 建连保守计入三次外呼额度，额度已用完。真实无效凭据请求未执行，2.4 保留 partial。
- 当前产品摘要 `9274e567144c8d5cdf6651cf39e1ddf525d2a88bccc7353f44d82a06c9f4f3f3`，源码/测试摘要 `8ec16533d34d2e41bbe80bb4746c68f4be12c1f91bee3f88cff7da6c7ab81a36`。
- 证据索引：[PRV-01–PRV-04 与当前结论](../../../../docs/development/V021_M5_CLOSEOUT.md)。完整 CI 首轮两项 Web 超时保留，未改前端/超时，独立 19 项及原样完整复跑通过，首次超时根因未确定。
