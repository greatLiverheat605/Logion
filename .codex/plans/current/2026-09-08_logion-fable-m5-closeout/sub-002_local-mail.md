# M5 本机真实邮件验收子计划

- 状态：当前批次实收、API 续验、R-11 修复及真实原链接表单、完整 CI 和最终复核均完成；五封额度已用完，临时资源已清理。QQ 包装跳转和真实提交后最终 UI 仍 partial，本子计划不归档为完全验收。
- 授权：所有者明确同意将现有发信角色短期 STS 凭据加密转交本机、仅内存用于已批准五封测试邮件，并允许自行操作浏览器完成所需配置。
- 候选：R-11 新候选 18 产品 / 39 源码测试；本批 API/Worker 从原 17/37 候选启动，前端切换至修复候选，后端字节一致性单独核实。既有凭据注入接口和生产默认装配未改。
- 原生产部署、角色权限与安全组保持；不创建长期 AccessKey。真实 Provider 三次额度已尽，不再请求。

## 步骤

1. 已完成：独立本地数据库、加密 Outbox、当前 API/Worker 与受控邮件入口就绪；离线演练 58 项通过，五次模拟请求不计入真实额度，演练资源已清理。
2. 已完成：本机仅内存私钥、云端公钥加密转交成功；现有角色 IMDSv2 读取退出零，本机解密及短期有效性检查通过。凭据仅内存注入，未改生产服务、权限或网络配置。
3. 实收与 API 已完成：五封已逐一核实来源、时间和正文，累计 5/5；首封过期证据保留，后四封的注册有效、恢复过期/有效和安全通知续验 56 项通过。真实表单读取 token 通过，提交后最终 UI 未验，QQ 包装跳转 404 保留。
4. 清理与最终复核已完成：finish 后凭据进程/API 停止，独占数据库、Redis 和卷删除；正常预览保留。R-11 完整 CI 首轮两项 UI 等待失败、独立 19 项通过，原样复跑八门通过；50 文件、111 链接、保护/候选/秘密/Run 校验通过，319 后端文件与最终候选一致。

## 证据边界

- 本机 API、加密队列、Worker 签名/HTTP 和实际投递绑定当前候选。
- 默认 ECS IMDS 装配不由本机凭据注入结果代替；云端取凭据事实单独记录。
- 阿里云接受请求不等于收件箱到达；未获得真实收件证据时不把投递项记为 passed。
- 过期通过独立测试数据设置到期状态，不改变生产主机时钟或认证代码；注明与自然等待到期的区别。
- 邮箱、发信地址、角色凭据、短期 token、私钥及原始云端输出不进入仓库/Run。

## 执行记录

- 2026-09-08：本机邮箱 UI 已核实首封和第二封的发件人、到达时间与正文。累计真实发送二封、剩余三封；第二封已计收但未完成注册。原按钮在服务存活时仍跳转到 QQ 404，不能归因于旧入口清理。直接原链接被共享前端解析器误判无效，R-11 修复在本子计划扩展，不再嵌套子计划；旧候选及通过记录保留，新产品须重新固定和验证。
- 2026-09-08：仓库外离线执行器完成 58 项检查并退出零；覆盖五次模拟签名/发送、注册/恢复有效与过期、重放、会话撤销、安全通知及队列清理。
- 2026-09-08：真实凭据加密转交、本机候选 API/Worker 身份核验通过；首次注册请求返回统一 202，Worker 真实调用接受成功，sent Outbox 密文载荷已清除。累计真实请求一、已核实收件零；整批尚未完成。
- 2026-09-08：云端发送详情确认该封邮件投递成功、接收服务器返回 250；本机独立数据库将对应 token 设为两天前过期后，API 400、错误码、未使用 token 和未验证账号状态通过。未声称自然等待到期或实际收件箱内容通过。
- 2026-09-08：本批停在收件箱登录前，已观察 13 项检查通过；通过标记只表示已执行断言，未运行整批 finish，不算五封完成。退出零，凭据进程、API、两个独占容器及卷均清理；剩余额度四封。

## 恢复入口

- 当前：真实额度已用完，不再运行任何 register/recover/notify。仓库外 `m5-local-mail-zRvCiv` 保存本批 56 项检查、实收观察和清理，token 与凭据不持久化。R-11 候选完整 CI 和最终复核已过；以下为上一轮恢复安排的历史，不再据此启动四封批次。
- 仓库外 `m5-local-mail-9fJnfp/batch-status.json` 保存首封 token 摘要和本机 Origin，未保存原始 token、邮箱或云凭据。登录后可核对首封实收链接与该摘要；首封临时入口已关闭，原过期 API 结果只按历史证据引用。
- 开始下一批前收件箱必须已可读。使用相同固定候选和独占数据，将剩余四封上限写入执行器，依次验证注册有效、恢复过期、恢复有效及安全通知；不得原样重启五封流程或重置总额度。仍使用已经批准的短期凭据转交，不重复索取授权。

## 首封跳转反馈与恢复修正

- 用户报告点击确认后在邮箱服务包装的跳转地址报网页不存在；解码其提供的地址后，原本机 Origin、验证路径和 token 摘要与 MAIL-02 首封一致。用户实收反馈已确认，但本机邮箱 UI、发件人和精确到达时间仍未观察。
- 该报错尚未进入 Logion，不能当作认证 API 拒绝。原本机临时入口又已在用户点击前停止，因此即使还原原链接也不能完成页面验证；这是验收编排的时序问题，原到达与 API 过期证据不因此变成页面通过。
- 本机链接须由运行项目的电脑打开；手机上的本机地址属于手机。下一批先确保本机邮箱可读，再启动、发信并保持入口运行至收件和页面核验结束。原邮件按钮的跳转异常仍单列，直接打开核对后的原链接仅用于本机业务诊断。
- 仓库外执行器现硬限四封，记录前批一封与累计数；未确认实收不得过期或完成，不允许跳过上一 action 或重复计入同封；只有 finish 完成才将整批 passed 设为 true。四封离线演练 53 项通过，退出零、独占容器及卷已清理，未新增真实发信。
- 证据：`m5-local-mail-9fJnfp/owner-reported-receipt.json` 和 `m5-local-mail-ycU16L`；旧快照不改写。M5/M6 状态和真实已用一封、剩余四封保持。

## 本轮完成记录

- 2026-09-08：R-11 旧实现新增回归 5 passed / 3 failed，修复后解析器 8 项和公开表单 4 项通过。第二封原链接及两封恢复原链接在修复前端显示表单并清除 fragment；浏览器新密码填写与提交未执行。
- 2026-09-08：后四封逐一实收与当前 action、批准发件人匹配，注册/恢复有效和重放、显式过期、登录、恢复会话撤销及通知正文均通过。56 项检查、finish 与退出零，两个独占容器及卷清理；五封累计完成，11.1/11.4 passed、11.2 partial，M5 未通过、M6 未授权。
