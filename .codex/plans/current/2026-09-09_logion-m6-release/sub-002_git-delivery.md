# M6 Git 交付与 PR 检查

- 父计划：[M6 发布计划](../2026-09-09_logion-m6-release.md)，步骤 2。
- 状态：执行中；所有者在具体交付提案后要求开始 M6，提交、推送现有开发分支及草稿 PR 已获准。
- 输入：已核验的 72 文件交付树 `2bf704726254258a85760834a95d953f60cdbb9c`，M5 基线与 R-14 产品摘要保持。
- 写入：独立交付副本中的选定文件与本步骤状态文档；原主树代码、T-04 保护修改及暂存区保持。

## 执行顺序

1. 复核批准树、逐文件摘要及远端分支；提交原 72 文件树，保存产品提交身份。
2. 单独提交本次授权与执行状态文档；推送现有 `dev/T-06-status-copy-consistency`，不强推。
3. 按仓库模板创建面向 main 的草稿 PR，核验真实 PR head/base 与完整差异；执行现有 PR 检查并处理确定性问题。
4. 整理完整差异复核、检查结果和具体合并提案；合并后实际 main SHA、四镜像及生产门禁由后续步骤处理。

## 实际记录

2026-09-09：输入清单与最终检查摘要匹配，远端开发分支仍为 M5 基线，main 未变。Git SSH 通道可读；命令行 GitHub 登录缺失或过期，浏览器已有仓库所有者会话可用。没有新增权限、凭据、Provider 请求或邮件。

2026-09-09：步骤 1 已完成，批准的 72 文件树原样提交为 `3620176704c27ecdef481aedee7fd62823f0718c`，提交后副本 clean。步骤 2 执行中，以单独文档提交记录授权和产品身份；新增本子计划后总选择 73 文件、完整 main 差异 161 文件。推送、草稿 PR 和远端检查尚未记为通过。

2026-09-09：步骤 2 已完成，文档提交 `3fc558d92b593b8b6895a338e2e4ee1424cb767d`
推送成功；步骤 3 已创建[草稿 PR #233](https://github.com/greatLiverheat605/Logion/pull/233)。
首轮数据库集成、浏览器和移动构建通过，快速检查在依赖审计失败；六项漏洞本地复现。
另复现普通冲突发送空删除时间的兼容缺陷，新增回归一失败一通过。独立副本执行最小
序列化修复及必要依赖补丁，随后重跑完整门禁。旧失败保留，尚未进入合并决策。

## 停止线

本地补丁审计零漏洞，普通 Push 序列化 9 项及完整 CI 八门通过（Web 430、
Python 614 / 120 deselected、合同零生成差异）。跨版本普通冲突通过，真正删除
冲突仍需 [sub-003](sub-003_old-client-compatibility.md) 的策略决定；修复提交的
远端检查待实测，PR 保持草稿。

2026-09-09：修复提交 `c134bf4` 的 fast/browser/mobile 已通过；integration 为
119 passed / 1 failed，旧空字段索引断言已改为字段缺席。全新隔离库完整集成
120 passed，迁移/清理通过，首次镜像启动失败保留。本地产品完整 CI 不重复，
提交该单条测试与说明文档后等待新 SHA 的远端门禁。

2026-09-09：集成修复 `7e6ce8b` 推送后，远端 integration/mobile 成功，fast
恢复表单聚焦时序失败；browser 为 131 passed、2 failed、2 flaky、10 skipped，
WebKit axe 扫描出现上下文销毁。仅在测试等待实际焦点后，定向 9 项和 Web 430 项
通过。工作流改用 Docker 相同的生产 standalone，工作流工具 4 项及格式/空白检查
通过，完整浏览器复验待环境补齐；首次缺失浏览器及系统依赖失败已独立保留。
该阶段仍在本子计划内修复与复验，不增加嵌套子计划或放宽 axe/焦点/重试要求。

- 远端分支发生非预期变化时先比较归属，不覆盖或强推。
- CI 失败按实际原因修复或记录，不把本机旧结果冒充远端通过。
- 不自动合并或部署；批准过的 Git 交付无需重复询问。
