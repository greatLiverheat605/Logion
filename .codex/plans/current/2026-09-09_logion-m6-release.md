# Logion M6 发布计划

- 状态：执行中，本地交付准备已完成；步骤 2 的提交、推送和草稿 PR 已获准并开始执行，合并及生产变更仍有独立前置条件。
- 来源：所有者询问推进 M6，沿用自主推进、仅必要决策询问的要求；[发布准备](../../../docs/development/V021_M6_RELEASE_PREPARATION.md)。
- 历史基线：`bac2a4371ce6a12d6c3e9a6124104d121b0f8807`；初始交付保持 R-14，后续安全与兼容性修复另固定候选和检查。
- M5：按批准范围通过，50/58 passed；D-15 例外、D-16/D-06 延后、D-17 豁免继续有效。
- Run：继续在已验证 `run-v021-fable-m5` 追加准备决策，基线不变；最终 Git SHA 变化时另记录交接，不覆写历史。
- MCP 任务工具不可用，同步本地计划和 Run，不补造任务完成或模型证明。

## 步骤

1. 本地准备已完成：[sub-001 候选准备](2026-09-09_logion-m6-release/sub-001_candidate-preparation.md)，72 文件交付选择、160 文件完整 main 差异及摘要已保存；发布工具 21 项、133 链接、格式/秘密/范围/Run 核验通过。
2. 执行中：[sub-002 Git 交付](2026-09-09_logion-m6-release/sub-002_git-delivery.md)，草稿 PR #233 的 `7333ead` 已通过同 SHA 四项远端检查；[sub-003](2026-09-09_logion-m6-release/sub-003_old-client-compatibility.md) 的 D-18 推荐方案已获批准并完成实现，正在完成新候选完整验收。之后再批准合并、固定实际 main SHA，取得同 SHA Main/capacity/Release 证据和四镜像 manifest。
3. 待前置：生产只读预检与具体回滚方案；获得维护/停写/切换批准后生成加密备份、同步异机、独立恢复、部署并执行授权 smoke。
4. 待部署后：至少 24 小时观察，核对资源/备份/告警/邮件/同步，取得生产完成批准；旧版本清理另遵循手册，不自动删卷。

## 停止条件与保留

- 不提交保护的 T-04 改动，不 reset/clean/stash 原工作树，不把用户持久化数据当作临时数据清理。
- 不沿用旧 SHA 的工作流、镜像或本机 M5 测试当作 M6 生产验收。
- 不输出配置值、凭据、用户内容；私有连接与本机环境细节只在仓库外保存。
- Provider/邮件旧额度不重置，本轮外呼零；具体 M6 批次按实际环境准备后批准。
- 所有新生产动作必须绑定候选、环境、窗口和回滚方案；缺项时继续独立本地准备，暂停依赖动作。

## 执行记录

2026-09-09：只读确认开发基线领先 main 九个提交，远端两个分支可读；最新成功 Main/capacity/Release 均旧 SHA。CLI 未登录，已通过公开 API 核实运行状态。创建发布准备文档和直接子计划，未创建提交、PR 或部署。

2026-09-09：独立交付包本地核验完成，53 份源码测试与 19 份文档已准确选择，R-14 产品不变、主树与保护修改保持。21 项发布工具测试通过；文档链接、格式、秘密、Git 空白和 Run 通过。进入具体 Git 授权门：提交该包、推送现有开发分支、创建面向 main 的草稿 PR；合并、生产动作和新增外呼另行处理。

2026-09-09：所有者要求开始 M6，具体 Git 交付已获准。批准的 72 文件树将先形成独立提交，再用文档提交记录当前授权与执行身份，避免改变已审阅产品。新增直接子计划，远端基线及批准树摘要复核一致，进入提交与草稿 PR 流程。

2026-09-09：草稿 PR #233 已推进至 `7e6ce8b`，依赖安全与集成断言修复已推送。
最新同 SHA 集成和移动成功，fast 暴露焦点断言时序，browser 暴露 WebKit axe
上下文销毁。等待焦点的测试修复已通过 Web 430 项；正在复验生产 standalone
浏览器工作流，首次本机缺环境失败保留。步骤 2 继续执行，D-18 策略尚待答复，
原基线/保护边界及生产停止线保持。

2026-09-09：六文件 CI 修复推送为 `7333ead`，同 SHA fast/integration/browser/
mobile 全部成功，生产浏览器 135 passed、10 项既定跳过、零失败或 flaky。本机
依赖安装与浏览器启动渲染通过；采用完整远端结果，不重复本机整套测试。候选副本
保持 clean；检查后的本地状态记录留待下一次交付同步。步骤 2 的独立 CI 修复完成，
D-18 仍等待所有者决定，不将等待当作批准，后续合并和生产步骤保持前置条件。

2026-09-09：所有者“采取推荐方案”，D-18 已批准并实现能力协商、旧端整批回滚与
升级控制、旧快照重建保护、新端原队列/游标恢复和页面提示。真实旧源码跨版本
17 项、当前 offline 定向 25 项、Web 定向 23 项及类型检查通过。完整集成先为
132 passed；补充首次分块初始化后兼容项通过，但 TOTP 和邮件重试出现时间相关
失败，带时钟监测复验中。上一提交绿灯不替代本次完整 CI 与新 SHA 远端检查。

同日复验：完整隔离集成 133 passed，实际监测到约正负 11 秒的时钟调整；历史
失败保留，认证/邮件逻辑与断言不变。累计 46 份实际 API 响应经旧校验器通过，
17 项跨版本检查再次通过；进入新提交与整仓/远端门禁。
