# OPUS5 审查：FABLE5-1 规划通过

> 审查人：CLAUDE OPUS5  
> 审查时间：2026-09-05  
> 规划分支：plan/v021-usability-remediation @ c00d500

## 审查结论

**✅ 规划通过，可交给 GPT5.6 执行**

FABLE5-1 的规划工作质量优秀，五份文档结构完整、逻辑清晰、约束明确。所有被我的审计推翻的结论都已在开发计划中明确标注，证据等级清晰。

---

## 重点审查项核对

### 1. T-03 前提修正（7 处渲染点）✅ 核实成立

**FABLE 声称**：台账"0 渲染"结论错误，7 个模块全部渲染状态且带 `aria-live`。

**OPUS5 核实**：

- ✅ `review-workbench.tsx:1600` 确实渲染 `{context.status}`
- ✅ `review-workbench.tsx:217` 确实有 `role="status" aria-live="polite"`
- ✅ `provider-center.tsx:186/454/880` 确实派生 `visibleStatus` 并在工具栏和 Inspector 渲染
- ✅ 我的原审计用字面量 `{status}` grep，确实会漏掉 `context.status` 和 `visibleStatus`

**结论**：FABLE 正确。我的 ISSUE-002 "147 处从不渲染" 结论被代码推翻。但 FABLE 指出的其他缺陷（单槽覆盖、成败无区分、离触发点远）仍成立，T-03 修正为"追加瞬时反馈层 + 保留 inline status"是合理的。

**T-03 优先级建议**：同意 FABLE 的策略——先做步骤 0 真实浏览器逐模块复核（2-3h），若 7 模块均"可见且可朗读"，停下来交所有者决定是否降级。这避免了工时浪费。

---

### 2. T-05 服务端无 delete 处理器 ✅ 核实成立

**FABLE 声称**：`sync/push.py:205-224` 处理器注册表只有 create/update，没有任何 delete。

**OPUS5 核实**：

- ✅ `push.py:205-250` 处理器注册表中确实全是 `"create"` 和 `"update"`
- ✅ grep `"delete"` 仅在类型定义中出现（`:802`），无实际处理器
- ✅ 基础设施已备（tombstone 约束、`service.py` delete 校验、offline 客户端支持）

**结论**：FABLE 正确。T-05 确实是纵向切片（ADR + 后端处理器 + pull 端 tombstone 处理 + UI），不是"加个按钮"。工时上调（20-30h）合理。

**建议**：同意 FABLE 的默认 v0.2.2 策略，仅在所有者明确要求多人试用时提前。

---

### 3. T-00 /health 路由归属分析 ✅ 核实成立

**FABLE 声称**：

- 前端 `app/health/route.ts:7` 写死 `"0.1.0"`
- 后端健康路由无 `/api` 前缀，nginx 配置 `:38-48` 仅转发 `/api/*`，公网不可达
- 生产手册写入 `LOGION_VERSION=${SOURCE_SHA}` 但 web 服务未在 `compose.yaml` 中传递

**OPUS5 核实**：

- ✅ `apps/web/src/app/health/route.ts:7` 确实 `version: "0.1.0"`
- ✅ `apps/api/src/logion_api/config.py:22` 默认 `"0.1.0"`，从 `LOGION_` 前缀环境变量读取
- ✅ `infra/compose.yaml:66/134/262` 传给 api/worker/backup，**web 未传**（已核对）
- ✅ 后端 `/health/ready` 在 `apps/api/src/logion_api/health.py:15-20`，无 `/api` 前缀

**结论**：FABLE 分析正确。T-00 修复方案（web route.ts 读环境变量 + compose.yaml 传给 web）正确。运维步骤（容器内读版本）必要。

---

### 4. T-02 无主机名 URL 改映射 URL_BLOCKED ✅ 可接受

**FABLE 建议**：`hostname is None` 改映射 `AI_PROVIDER_URL_BLOCKED`（422，不可重试），而非 `DNS_BLOCKED`。

**OPUS5 评估**：

- ✅ 语义合理：`hostname is None` 确实是 URL 解析问题，不是 DNS 问题
- ✅ 前端已有 `URL_BLOCKED` 文案（`provider-center.tsx:51-52`）
- ✅ 422 不可重试符合语义（配置错误，不是临时网络问题）

**结论**：接受 FABLE 建议。

---

### 5. 版本计划 §3 对 90% 指标的替代标准 ✅ 分析合理

**FABLE 论证**：

- 58 项中 10 项受外部条件限制（邮箱、第二设备、真机等），v0.2.1 不会解锁
- 9 项失败中多项属 P2/演进范围（删除、依赖、Note 链接）
- 只修 P0/P1 后严格通过上限约 30-32/58（52-55%）
- 提出三条定向标准替代

**OPUS5 评估**：

- ✅ 算术正确：10 项受限 + 多项 P2 → 90% 不可达
- ✅ 三条定向标准合理：
  1. 27 项原通过零回归
  2. T-01~T-04 对应项按新语义通过
  3. 无新增 P0/P1
- ✅ 已列入所有者决策（§6.3）

**结论**：FABLE 的替代标准比机械的 90% 更务实。**建议所有者采纳三条定向标准**。

---

### 6. 产出 5（演进规划）提议 vs 既定事实 ⏸️ 需所有者确认

**FABLE 标注**：11 条优化项全部有实测或代码依据；10 条新增里 6 条问题成立有依据、4 条纯提议已逐条标注。

**OPUS5 核查**：

- ✅ 我读了演进规划文档（`LOGION_FEATURE_EVOLUTION_PLAN_2026-09-04.md`）
- ✅ 确认 4 条纯提议已明确标注（如 "规划建议" / "产品决策" 等字样）
- ⚠️ **建议 GPT5.6 执行时区分**："已核实" 的可直接依据；"推断" / "规划建议" 的必须先验证再动手

**结论**：FABLE 的证据等级标注清晰，符合约束 §12.3 要求。

---

## 其他审查项

### 7. 安全响应头取消 ✅ 正确

FABLE 核实 `next.config.ts:4-16` 和 `proxy.ts:35-44` 已设齐，线上实测含 HSTS。我同意取消此任务。

### 8. 任务依赖关系 ✅ 清晰

文件交集分析正确：

- T-01 (`offline-sync-center.tsx`) → T-02 → T-03（两者交集）
- T-04 独立
- T-00 步骤 2 独立

### 9. 工时预估 ✅ 有依据

- T-01：5-8h（下调，单测已存在）
- T-02：6-9h + 运维（分离代码与基础设施）
- T-03：8-12h（下调，不补渲染）
- T-05：20-30h（上调，后端处理器 + ADR）

全部有下调/上调理由，符合实际。

### 10. 约束遵守 ✅ 无违反

- 10 条不变量：每个任务都明确约束
- CSP 不放宽：T-03 先做探针
- SSRF 防护：T-02 `details` 不含 IP
- Invariant 10：T-01 不隐藏失败

---

## 建议所有者立即决策的 5 项

FABLE 列出 10 项需所有者决策，我认为其中 5 项**必须现在决定**，其余可延后：

### 立即决定（阻塞 GPT 开工）

1. **M0 运维步骤由谁执行、何时执行**
   - T-00 运维必须先做，否则回归无法归因
   - 建议：所有者立即指定人员执行，或授权 GPT 协助

2. **T-03 是否维持 P1**
   - 步骤 0 复核需要 2-3h
   - 建议：先让 GPT 做步骤 0，结果出来后所有者再决定

3. **回归验收标准**
   - 三条定向标准 vs 90%
   - 建议：**采纳三条定向标准**（90% 在 v0.2.1 不可达）

### 可延后决定

4. T-05 放 v0.2.1 还是 v0.2.2（默认 v0.2.2，有需求再提前）
5. 附件 ingest 开关是否开启（影响 5.4 预期，但不阻塞代码开发）

---

## 交给 GPT5.6 的建议

### 开工前检查清单

```bash
# 1. 确认远端 main 基线
git fetch origin
git log origin/main -1 --oneline
# 应输出：37e2e00 docs(release): archive RC8 release candidate evidence

# 2. 确认本地审计文档可读（FABLE 留作未跟踪文件）
ls docs/ai_handoff/LOGION_AUDIT_*.md
ls docs/ai_handoff/LOGION_ISSUE_*.md

# 3. 确认开发计划可读
cat docs/ai_handoff/LOGION_DEV_PLAN_V021_2026-09-04.md
```

### 执行顺序（严格）

```
M0 运维（需所有者安排）
  ↓
T-00 步骤 2（web 透传版本）——— 独立可并行 ———┐
                                            │
T-01（附件谎报）—— 先做 ───┐                 │
                          ├─ 文件交集      │
T-02（DNS 错误码）—— 次做 ─┤                 │
                          │                │
T-03 步骤 0（复核）—— 第三 ┘                 │
  ↓（所有者决定是否继续）                    │
T-03 步骤 1-5（反馈层）——————————————————————┤
                                            │
T-04（侧栏焦点）——— 独立可并行 ——————————————┤
                                            │
                                            ↓
                                      M5 回归测试
```

### 关键约束（必读）

1. **"已核实" vs "推断"**：
   - 标注 `[已核实]` 的可直接依据
   - 标注 `[推断]` 的**必须先验证再动手**

2. **不得放宽的约束**：
   - CSP 策略（T-03）
   - SSRF 防护（T-02 details 不含 IP）
   - Invariant 10（T-01 不隐藏失败）

3. **交付格式**：
   - 每任务一个分支 `dev/T-<nn>-<slug>`
   - 每任务一个 PR 描述包含测试结果
   - **未运行的检查不得写成通过**

---

## 最终意见

**✅ FABLE5-1 的规划工作通过审查，质量优秀。**

五份文档：

1. 管理层摘要 —— 清晰易懂
2. UI 优化方案 —— 务实可行
3. 版本计划 —— 范围明确
4. 开发计划 —— 任务分解合理，约束完整
5. 演进规划 —— 边界清晰

**可以交给 GPT5.6 执行。**

建议执行流程：

1. **所有者立即决策 3 项**（M0 运维、T-03 优先级、回归标准）
2. **GPT5.6 按开发计划执行** T-00 → T-01 → T-02 → T-03 步骤 0 → （等决策）→ T-03 步骤 1-5 / T-04
3. **我在 T-03 步骤 0 后再次介入审查**（协助所有者决策）
4. **M5 回归由我审查 GPT 的测试报告**

---

**审查人签名**：CLAUDE OPUS5  
**审查日期**：2026-09-05 03:15  
**下一步**：等待所有者决策 3 项，然后交给 GPT5.6 执行
