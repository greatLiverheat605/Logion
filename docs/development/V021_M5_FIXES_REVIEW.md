# v0.2.1 M5 缺陷修复与复验

> 日期：2026-09-07。状态：R-01–R-03 已修复并完成本地复验，未提交，M5 门仍未通过。
> Git 基线：`bac2a4371ce6a12d6c3e9a6124104d121b0f8807`，未创建新提交。
> 前轮完整结果：[M5 本地回归记录](./V021_M5_LOCAL_REGRESSION.md)。
> 本文保留 R-01–R-03 阶段历史。R-05 慢保存竞态、R-06 弹窗对比度、R-07 时钟回拨的后续结果见 [M5 收口记录](./V021_M5_CLOSEOUT.md)。

当前 [58 项清单](./FABLE_REGRESSION_CHECKLIST.md)为 43 passed、1 failed、5 partial、
3 blocked、6 out_of_scope。6.2、8.3、12.1 更新为 passed，原范围内 26 项当前映射均
passed；其他项继续引用前轮证据，本轮没有重新执行全部 58 项。13.3/R-04 仍失败，
13.1 侧栏仍 partial，不能以通过数上升替代 M5 的全部验收门。

## 范围与实现

所有者在前轮修复顺序后回复“开始”，授权修复 R-01、R-02、R-03 并复验。T-04 三个
受保护文件继续保持原样；没有提交、推送、合并、生产发布、真实外呼或敏感开关授权。

| ID   | 根因                                                                          | 本轮修改                                                                       |
| ---- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| R-01 | 保存成功清空父组件的题目，但 AnswerSheet 自身仍挂载，内部 review 阶段跨次保留 | 仅在题目存在时挂载 AnswerSheet，关闭后销毁阶段和表单；保存失败继续保留输入     |
| R-02 | 互操作页仍重复使用“创建加密导出”，遗漏 Data 已有的存储/下载区别               | 按钮改“创建数据导出”，说明服务端加密存储、下载为未加密的可读 ZIP，备用路径同步 |
| R-03 | Sonner 浅色成功文字默认颜色对浅底仅约 4.25:1                                  | feedback.css 以同等优先级覆盖浅色成功文字为 #006b2c；深色规则保留              |
| R-04 | 画像入口可见文本与 accessible name 不匹配                                     | 涉及 T-04 保护文件，本轮只记录，未修复                                         |

R-01 不修改服务端 bool、同步合同或未知结果语义。新单测覆盖成功重开、失败保留与
Escape 后重开；浏览器补保存、Escape、非空草稿取消三条重开路径。
R-03 新浏览器测试要求真实同步成功 Toast 显示时执行 axe，并覆盖 320/1440px 两主题。

## 候选与验证记录

独立检出完整基线，覆盖明确列出的三份产品文件及相关测试。候选依赖从锁文件和本地
缓存独立安装，未复用或删除主工作树 node_modules。候选不含 T-04 工作树改动。
产品覆盖摘要为 `9117a37769f0b44c4b56406811dac5d7fb3df5577f4a1c83edac0968893ff9aa`，
Web 运行版本会同时标注完整基线和覆盖摘要前缀，不冒充裸提交。
本地 Web 为 3003 端口，实际 health 返回完整基线加 `+m5-9117a37769f0`；复用已清空的
M5 隔离数据库、API 与 Worker，后端源码仍为原完整基线，未改变邮件 disabled 或敏感能力。

本机证据组为 `m5-fixes-GSr4mV`，运行日志、截图和临时脚本均留在仓库外。

| 检查                | 已观察结果                                                                                                                    |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| R-01 单测复现       | 首轮两例因重复按钮定位失败；限定 review-answer 后，保存重开用例出现预期的缺少“提交回答”失败，另 11 项通过                     |
| 定向单测            | 修复后 Review 12、IntegrationHub 7，共 19 passed；后续强化失败保留与取消断言                                                  |
| 完整 CI 首轮        | Web 399 passed / 6 failed；存在等待元素和 5 秒超时，门禁退出 1                                                                |
| 限制 CPU 并行度重跑 | Web 405 项通过；Python 605 passed / 1 failed / 109 deselected，健康检查受启动器的候选版本环境变量影响                         |
| 最终完整 CI         | F01：移除测试阶段版本覆盖后，完整八门退出 0；Web 405、offline 74、contracts 13、mobile 4、Python 606/109 deselected、mypy 181 |
| 浏览器              | F02–F04 全部通过，分组和边界见下表                                                                                            |

| ID  | 仓库外证据                                 | 实际结果                                                                                                                  |
| --- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| F01 | `ci-fast-limited-1788769076365.log`        | `taskset -c 0-3 pnpm ci:fast` 完整八门通过，含强化后的失败保留单测；合同生成干净树检查退出 0                              |
| F02 | `fixes-ui-1788769291070/results.json`      | 20 passed：两主题成功 Toast 的 320/1440px axe、现有 12 项 mock 失败反馈、5 项互操作真实流程及错误边界、Review 全断点/重开 |
| F03 | `fixes-offline-1788769385461/results.json` | 2 passed：真实日程、同题两次离线保存、未知状态、重连后两条 bool 记录分别 false/true                                       |
| F04 | `review-light-1788769457939/results.json`  | 1 passed：独立账号浅色 Review 四断点、重开和 axe；F02 的 Review 因前例主题偏好为深色，故补此组                            |
| F05 | `cleanup-initial.json`、`cleanup.json`     | 两次清理累计 3 个合成用户及相关数据；每次 91 张业务表总行数 0，Redis 键 0；认证文件由 teardown 删除                       |

三组浏览器使用 Chromium、单 worker、trace off；F03 为复用的仓库外补验脚本，不改变
产品代码。F02 真实下载仍检查 ZIP 的 SHA-256 与 manifest.json、data.json、notes.md、
tasks.csv、papers.bib，近期认证错误明确 mock。组间有 Review 重跑，不累加为通过率。
成功 Toast 四张截图和 Review 两主题桌面/最窄截图已查看；最终浅色四档截图保留在候选
reports/ui-refactor/after 中。原失败日志没有删改；API 刚启动时发生的一次 readiness
拒绝连接与注册 500 属初始化竞态，健康检查返回 200 后才开始业务测试。

每轮 CI 保留原日志。重跑前只移除本轮独占候选里生成的 offline coverage，避免已知
guard:context 扫描生成报告中的本机路径；主工作树及其他所有者的文件不清理。

## 独立审查与剩余边界

两个工具子代理只读复核，主线保持唯一 writer。R-02 文案范围和 R-03 CSS 优先级合理；
浅色新颜色理论对比度约 6.34:1，现有深色约 9.44:1，F02 显示中 Toast 的 axe 已通过。
R-01 未发现新增确定性产品回归，主线按建议补强当前弹窗存在性与非空取消回归断言。

慢保存时关闭并重开，旧异步回调随后关闭新弹层的竞态在原实现中已存在，当前未复现或
修复，单列后续调查。上轮 Mastery/Sync 组合失败仍未定根因：共享服务端数据累积与
测试中宽泛的 mock/选择器可确认，但不足以证明具体本地校验失败原因。孤立通过不等于
组合稳定性通过，不把只读建议当作已实施修复。

M5/M6 门保持未通过；保留生产、邮件、真实读屏和移动延后的边界。未发现可确认为本轮
新增 P0/P1 的证据，不将未测边界解释为安全通过。主线修改只有三份产品文件及相关测试，
完整工作树与候选差异、秘密扫描、文档链接/计数和 T-04 摘要在收口时独立复核。
测试账号、关联数据和临时认证已清理，注册额度恢复为每小时 5；保留空本地测试栈与
仓库外脱敏证据供复查。没有新 commit、push、merge、部署或敏感能力启用。

最终主线复核已通过：15 份交付文件的 Prettier、63 个本地链接、58 个唯一 ID/状态统计、
原 26 项映射、Gitleaks、diff 空白检查、9 份源码/测试与实际候选字节一致、产品覆盖摘要一致。
T-04 三文件 SHA-256 与开工记录一致，且其未提交改动不在隔离候选内；暂存区为空。
状态账本保留旧事件与旧结果，只追加本轮授权和实际观察，不编造旧角色配置下的模型证明。
当前 Run validator 通过。最终独立只读复核未发现文档错误或遗漏，已交叉核对 58 项统计、
三组浏览器结果和两份清理记录；没有把该复核描述为重新执行产品测试。
