# FABLE 候选交付范围与后续验收

日期：2026-09-07。交付基线：`14fd3eb2b81869ddf967cb32b9e9fe8e2d33f2bf`。
目标分支：`dev/T-06-status-copy-consistency`。

## 本次 Git 交付

用户在“下一步是 Git 交付及最终版本、验收口径确认”之后回复“继续”，本次据此交付
已经验证的 StudySession、T-05a 和 FABLE 归纳。只向当前开发分支交付；不合并 main、
创建 release、部署或修改生产开关。各专项报告中的“未提交”保留为验证阶段历史。

交付内容为 12 份产品源码/测试/测试发现配置，2 份专项报告，6 份 FABLE 原文与规划审查，
2 份计划/回归映射，5 份持久状态或入口文档，以及本文件，共 28 个文件。
当前未跟踪的 T-04 报告、三个受保护 T-04 文件、其他本地材料和运行证据均不入提交。

产品候选与已通过浏览器验证的 12 文件清单字节一致，摘要为
`c3215c8951b7056ed7d6dff19b536f87f9fed33c9e5e0445e374b7ef8cf52fea`。
实际检查、失败记录、真实/mock 边界和清理结果见
[StudySession 报告](./V021_T06_STUDY_SESSION_REVIEW.md)与
[T-05a 报告](./V021_T05A_ATTACHMENT_QUEUE_REVIEW.md)。

提交 SHA 以包含本文件的 Git 提交为准，远端保存状态以同名分支实际比对为准；本文件不提前
声明 push 已成功。开发分支交付不等于 M5 验收通过或 M6 发布授权。

## 提交前复核

2026-09-07 主线再次实际运行完整 `pnpm ci:fast`，聚合退出 0，八门全部通过：
Web 403、offline 74、contracts 13、mobile 4、Python 606 passed / 109 deselected，
mypy 181 文件、协调测试 118。日志保留在本机证据组 `t05a-verify-DMBKxD` 的
`ci-fast-delivery.log`。测试中的既有 Select 警告仍按专项报告记录。

文档结构检查再次确认全部 58 个原 ID、12 个补充项、原通过基线分组和恢复正文一致；
28 文件组成的候选树内链可解析。只读复核发现总表仍留旧阶段的“待审查/Git 授权”，
已更新当前表述，并将归纳阶段检查明确保留为历史快照。此文档收口未修改 12 份产品文件，
原隔离浏览器证据仍绑定相同文件摘要，本轮没有重跑或新增 M5 全量结果。

## 待确认决策

以下是具体推荐稿，状态为待 owner 确认，不改写原始 FABLE/OPUS5 文档。

| ID   | 推荐决定                                                                                       | 仍保留的边界                                                                                     |
| ---- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| D-02 | 统一为 v0.2.1 修复候选：T-00–T-03，以及已提前批准的 T-05/T-05a/T-06                            | 版本归属不是生产验收；T-04、T-07 和未批准 O/N/U 演进不纳入本次施工，不修改既有产品版本配置       |
| D-03 | 采用下述三条定向标准及 D-06 的明确修订，替代笼统的 >90%                                        | 58 个 ID 与分母不变；受限项如实记录，不能移出分母制造通过率                                      |
| D-06 | T-04、真机与移动施工继续延后；原 27 项中的 12.2 移动导航保留为延后项，其他 26 项逐项核对零回归 | 13.1 桌面键盘可以验，但侧栏焦点未验时整项只能 partial；不得宣称原 27 项全部通过或原 M3/M5 已通过 |

S3 §3 的原三条标准保留如下：

1. 27 项原通过项零回归；
2. 与 T-01~T-04 直接对应的清单项（5.4、2.4、2.5、3.x、6.x、13.1）按“预期结果”判定通过或按新语义判定通过；
3. 无新增 P0/P1。

若 D-06 获批，新报告应把第一条写为“原 27 项中的 26 项零回归，12.2 按批准决定延后”，
第二条明确记录 13.1 的已验/未验部分。原标准及未满足事实继续保留。批准前，58 项当前
状态不因这份建议变成 passed 或 out_of_scope；H1 的审查建议也不代替 owner 决定。

## 下一阶段本地回归

1. 绑定实际交付 SHA，建立排除 T-04 候选改动的隔离 production build，使用合成账号和数据。
2. 检查 authenticated 项目明确启用；复用既有 Today/Records/Planning/Sync、feedback 和
   status-copy 浏览器回归。每次运行记录准确源码及命令，不把旧计数直接标为新 SHA 的通过结果。
3. 按 [58 项回归映射](./FABLE_REGRESSION_CHECKLIST.md)逐项执行可用条件下的路径。
   保留原 27 个 ID 的对照、Goal/Topic、外链/内链、ZIP/JSON 和既有邮件/邀请邮件的区别。
4. 六种结果状态合计必须为 58；A-01–A-12 单列。无前置条件填 blocked，未执行填 not_run，
   只有明确范围决定才可填 out_of_scope；mock 不替代真实持久化或 scanner 验收。
5. 输出绑定 SHA 的本地回归报告，列剩余问题、受限原因、证据和清理结果；不得直接升级为生产 acceptance。

OPS-01 生产 SHA/四镜像 digest/公网版本，OPS-02 ingest 实值，OPS-03/04 DNS/实际 Provider
链路，OPS-05 真实邮件仍需分别验证。开启 ingest 与只读观察是不同权限。OPS-07 的 NVDA/
VoiceOver 真实播报不能由 DOM、ARIA 或 axe 代替；移动真机、软键盘与移动读屏继续延后。
最终版本决定不自动豁免这些验收，也不授权 M6 生产变更。
